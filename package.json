{
  "name": "voxstrike",
  "version": "2.0.0-dev.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "check": "npm run build",
    "test:e2e": "playwright test tests/e2e/greenfield.spec.js --reporter=line"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "three": "^0.185.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "8.2.2"
  }
}
